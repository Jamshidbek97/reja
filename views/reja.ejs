<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reja</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body style="background-color: #b8bbd1">
    <div class="container" style="width: 75%">
      <h1 style="width: 75%" class="display-4 text-center py-1">
        Purchases and Purposes
      </h1>
      <div
        class="jumbotron p-3 shadow-sm"
        style="width: 75%; background-color: #cff4fc; border-radius: 5px"
      >
        <form action="/create-item" id="create-form" method="POST">
          <div class="d-flex align-items-center">
            <input
              type="text"
              id="create-field"
              autofocus
              autocomplete="off"
              name="reja"
              class="form-control mr-3"
              style="flex: 1; margin-right: 3rem"
            />
            <button class="btn btn-primary">Yangi reja Qo'shish</button>
          </div>
        </form>
      </div>
      <ul
        style="width: 75%; margin-top: 2rem"
        id="item-list"
        class="list-group pb-5"
      >
        <% if (items.length > 0) { %> <% items.map((item) => {%>
        <li
          class="list-group-item list-group-item-info d-flex align-items-center justify-content-between"
        >
          <span class="item-text"><%=item.reja%> </span>
          <div>
            <button
              data-id="<%=item._id%>"
              class="edit-me btn btn-secondary btn-sm mr-1"
            >
              Rename
            </button>
            <button
              data-id="<%=item._id%>"
              class="button delete-me btn btn-danger btn-sm"
            >
              Delete
            </button>
          </div>
        </li>
        <% })%> <% } else { %>
        <p>No items available</p>
        <% } %>
      </ul>

      <button id="clear-all" class="btn btn-danger">Delete All Items</button>
    </div>
    <script src="/browser.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  </body>
</html>
